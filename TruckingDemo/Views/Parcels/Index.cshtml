@model IEnumerable<TruckingDemo.Models.Parcel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TrackingNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CargoID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DestinationLat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DestinationLong)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TrackingNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CargoID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DestinationLat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DestinationLong)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParcelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParcelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParcelID })
        </td>
    </tr>
}

</table>
